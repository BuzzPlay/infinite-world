# API Contract

This package holds the stable wire contracts shared by the Web application and the Fastify API. Keep transport shapes and event names here; keep product-specific behavior in the owning application or API module.
