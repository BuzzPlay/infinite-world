# API Contract Package

This package contains the TypeScript request, response, and real-time event shapes shared by the Web application and the Fastify API. Keep provider-specific fields out of this package unless they are part of the stable Web workflow.
